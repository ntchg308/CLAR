package com.example.clar;

public class Glob {
    enum Direction{
        TOP, RIGHT, LEFT, BOT, NONE
    }

    enum DragPageState{
        TOUCH_HOLD,
        MOVE_PAGE_WITH_POINTER,
        HOME_PAGE_WRAPPER_HANDLE
    }
}
